@model List<ResultBrandDto>

@{
    ViewData["Title"] = "Brands";
    ViewData["h1"] = "Brand actions";
    ViewData["v1"] = "Dashboard";
    ViewData["v2"] = "Brand";
    ViewData["v3"] = null;

    var counter = 0;
}

<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead class="thead-dark">
                            <tr>
                                <th>#</th>
                                <th>Name</th>
                                <th>Image</th>
                                <th>Update</th>
                                <th>Delete</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var brnd in Model)
                            {
                                counter++;
                                <tr>
                                    <th scope="row">@counter</th>
                                    <td>@brnd.Name</td>
                                    <td>
                                        <img src="@brnd.ImageUrl" alt="Slider Image" width="100" height="60" />
                                    </td>
                                    <td>
                                        <a asp-action="update" asp-controller="brand" asp-route-id="@brnd.BrandId" class="btn btn-success">Update</a>
                                    </td>
                                    <td>
                                        <a asp-action="delete" asp-controller="brand" asp-route-id="@brnd.BrandId" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this brand?');">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a asp-controller="brand" asp-action="create" class="btn btn-primary">Create new brand</a>
                </div>
            </div>
        </div>
    </div>
</section>