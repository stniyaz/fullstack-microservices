@model List<ResultCargoCompanyDto>

@{
    ViewData["Title"] = "Cargo Companies";
    ViewData["h1"] = "Cargo Company actions";
    ViewData["v1"] = "Dashboard";
    ViewData["v2"] = "Cargo Companies";
    ViewData["v3"] = null;

    var counter = 0;
}

<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead class="thead-dark">
                            <tr>
                                <th>#</th>
                                <th>Name</th>
                                <th>Update</th>
                                <th>Delete</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var cmp in Model)
                            {
                                counter++;
                                <tr>
                                    <th scope="row">@counter</th>
                                    <td>@cmp.CompanyName</td>
                                    <td>
                                        <a asp-action="update" asp-controller="cargocompany" asp-route-id="@cmp.CargoCompanyId" class="btn btn-success">Update</a>
                                    </td>
                                    <td>
                                        <a asp-action="delete" asp-controller="cargocompany" asp-route-id="@cmp.CargoCompanyId" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this Cargo Company?');">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a asp-controller="cargocompany" asp-action="create" class="btn btn-primary">Create new cargocompany</a>
                </div>
            </div>
        </div>
    </div>
</section>